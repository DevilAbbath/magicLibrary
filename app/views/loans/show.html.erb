<p style="color: green"><%= notice %></p>

<%= render @loan %>

<div>
  <%= link_to "Return this Book", edit_loan_path(@loan), class:"btn btn-secondary" %> |
  <%= link_to "Back to loans", loans_path, class:"btn btn-primary" %>

  <%= button_to "Destroy this loan", @loan, method: :delete, class:"btn btn-danger" %>
</div>
